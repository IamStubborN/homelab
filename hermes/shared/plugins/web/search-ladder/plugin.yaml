name: search-ladder
version: 1.0.0
description: "OmniRoute household gateway web search + extract (OpenAI-compatible /v1/search + /v1/web/fetch). Auth uses the omniroute key exposed as OPENAI_API_KEY by the profile entrypoint."
author: iamstubborn
kind: backend
provides_web_providers:
  - search-ladder
