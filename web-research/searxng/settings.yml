use_default_settings: true

general:
  instance_name: Hermes Search

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  bind_address: 0.0.0.0
  port: 8080
  limiter: false
  image_proxy: false
